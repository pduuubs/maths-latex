

\documentclass[a4paper]{article}
\usepackage[]{pan}

\begin{document}
\fexos{Croissance compar\'ee}{\term S}

%%-----------------------
\Exo[]{
Calculer les limites des fonctions ci-dessous en $+\infty$ et $-\infty$
  \begin{enumerate}
  \item \qcmabc{$f(x)=\dfrac{x}{\e^x}$}{$f(x)=\e^x-x$}{$f(x)=x^2\e^{2x}$}
  \item \qcmabc{$f(x)=(x^2-x)\e^x$}{$f(x)=\dfrac{\e^x}{x^2+1}$}{$f(x)=x^2\e^{-x}$}
  \end{enumerate}
}
%%-----------------------
\Exo[]{
\begin{enumerate}
  \item Determiner la limite en 0 pour $x\in\into{0}{+\infty}$ de $f$
    d\'efinie par 
$f(x)=x\e^{\frac{1}{x}}$
\item D\'eterminer la limite en $+\infty$ de $f$ o\`u
  $f(x)=\dfrac{\e^x}{\sqrt x}$
  \end{enumerate}
}
%%-----------------------
\Exo[]{
}
%%-----------------------
\end{document}
